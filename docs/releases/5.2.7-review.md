# 5.2.7 review checkpoint

Branch: `release/5.2.7`, based on `b0fc4aa` (v5.2.6).

Implemented: color-first palette cards; account-only Save with a single pending request and
login/registration return; exact color-count filtering (1–8) with tags, search and URL history;
shorter editorial hero; guest mobile navigation; action disclosures; Favorites states; scoped
palette export links. Release notes and API documentation reflect the change.

Validation on 2026-09-14:

- Frontend: 232 tests passed across 42 files. Coverage passed all configured thresholds:
  lines 80.49%, statements 78.12%, branches 72.90%, functions 72.34%.
- Production build, TypeScript, Oxlint, Prettier and orphan CSS checks passed.
- Backend Ruff, formatting and mypy passed.
- The last Vitest run returned exit code 0 with a shutdown warning about a lingering Vite
  handle; confirm clean shutdown in CI.

Still required before merge:

- Run backend tests against the project's disposable PostgreSQL stack. Native PostgreSQL was
  unavailable in this session; the experimental local PGlite bridge did not complete the suite.
  Two new regression tests cover color-count bounds and combined filters before pagination.
- Inspect desktop/mobile light and dark UI, keyboard interactions and scroll restoration.
  The session's browser policy blocked both localhost and local HTML navigation, so no visual
  verification or screenshot-baseline updates were performed.
- Run Playwright E2E/a11y and review regenerated visual baselines in the pinned test image.
- User reviewed the interactive demos and authorized PR creation on 2026-09-15.

The separately supplied offline HTML review bundles these production components and styles
with sample palette data. It does not contact the backend or submit login/registration data.
Its layout has not been visually verified in this session. The source-code tests exercise the
real save/login flow with API mocks; the HTML is for design review only.


Follow-up fixes from mobile review (2026-09-15):

- Search hint measures available input width and falls back to `Search`; opaque hints and
  explicit header wordmark colours improve light-theme readability.
- Color-vision controls reflow on small screens and use a strong selected surface.
- `More` groups Edit a copy, Copy link and (for non-owners) Report palette.
- CSS chevrons avoid font-baseline drift; action menus clamp to both viewport edges.
- Path changes animate the incoming content for 240 ms; query-only filter changes do not
  restart the page animation. Header and footer remain mounted.
- Action/mobile disclosures animate open and closed, with closed content inert. Controls
  use short colour/background transitions; reduced-motion disables CSS motion throughout.

Final frontend verification on 2026-09-15 after the transition changes:

- 232 tests passed in 42 files; coverage command exited 0.
- Coverage: lines 80.52%, statements 78.19%, branches 73.02%, functions 72.61%.
- Production build (including TypeScript), Oxlint and formatting of changed source files passed.
- The same Vite shutdown-handle warning remains; backend/browser gates above remain open.
- Push to origin was attempted but failed because this environment has no GitHub credentials.
  The PR description is prepared in `docs/releases/5.2.7-pr.md`; no remote PR exists yet.

## Verification in the publishing environment (2026-09-15)

This is a separate run from the checkpoints above, performed in the session that imported the
reviewed `release/5.2.7` commit (`0bc2730`) from the handoff bundle and published the PR. The
import was verified 1:1: `git diff 0bc2730 HEAD` is empty and the range is 54 files,
+1854/-781 as recorded in the handoff. No source was hand-edited.

Frontend (`frontend-react`, Node 22, `npm ci` from the lockfile):

- `npm run build` (tsc + vite): passed.
- `npm run lint` (oxlint) and `npm run format:check` (prettier): passed.
- `npx vitest run --coverage --maxWorkers=2`: 232 passed across 42 files, exit 0. Coverage
  statements 78.19%, branches 72.95%, functions 72.61%, lines 80.52% — thresholds met. This
  run finished without the lingering Vite-handle warning seen in the earlier checkpoints.
- `npm run css:orphans`: no orphaned class names.

Backend (Python venv from `backend/requirements-dev.txt`):

- The Docker Compose `test` profile could not be used: the egress policy blocks the container
  image registry (`postgres:16-alpine` pull returns 403). Instead the suite ran against a
  locally provisioned PostgreSQL 16 with `pg_trgm`, which is the same engine the profile uses.
- `ruff check`, `ruff format --check` and `mypy backend/app`: passed.
- `pytest`: 192 passed, coverage 88.92% (gate 80%). This includes the new palette-API
  regression tests for exact color-count bounds (1/8), invalid bounds and combined
  search/tag/count filtering with total counted before pagination.
- Alembic agreement (drop schema → `alembic upgrade head` → `alembic check`): "No new upgrade
  operations detected."
- `pip-audit -r backend/requirements-dev.txt --strict`: no known vulnerabilities.

Playwright:

- `e2e/` (a11y + flows + focus + email links): 32 passed. The pinned `@playwright/test`
  `1.62.1` expects browser build 1234, which is not installed here; the environment ships
  Chromium build 1194. The suite was pointed at that binary through a temporary, uncommitted
  local config override (`launchOptions.executablePath`) — no committed config or app code was
  changed. CI runs the same suite in the pinned image.
- Visual baselines (`playwright.visual.config.ts`), the Lighthouse budget and the real-stack
  integration suite were not run here. Screenshot diffs are only meaningful in the pinned
  Playwright image with the matching browser build, so running them against build 1194 would
  only produce noise, and baselines must never be updated blindly; Lighthouse and integration
  need container images the egress policy blocks. These remain for CI to validate on the PR.
