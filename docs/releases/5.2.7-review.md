# 5.2.7 review checkpoint

Branch: `release/5.2.7`, based on `b0fc4aa` (v5.2.6).

Implemented: color-first palette cards; account-only Save with a single pending request and
login/registration return; exact color-count filtering (1–8) with tags, search and URL history;
shorter editorial hero; guest mobile navigation; action disclosures; Favorites states; scoped
palette export links. Release notes and API documentation reflect the change.

Validation on 2026-09-14:

- Frontend: 232 tests passed across 42 files. Coverage passed all configured thresholds:
  lines 80.49%, statements 78.12%, branches 72.90%, functions 72.34%.
- Production build, TypeScript, Oxlint, Prettier and orphan CSS checks passed.
- Backend Ruff, formatting and mypy passed.
- The last Vitest run returned exit code 0 with a shutdown warning about a lingering Vite
  handle; confirm clean shutdown in CI.

Still required before a PR is ready:

- Run backend tests against the project's disposable PostgreSQL stack. Native PostgreSQL was
  unavailable in this session; the experimental local PGlite bridge did not complete the suite.
  Two new regression tests cover color-count bounds and combined filters before pagination.
- Inspect desktop/mobile light and dark UI, keyboard interactions and scroll restoration.
  The session's browser policy blocked both localhost and local HTML navigation, so no visual
  verification or screenshot-baseline updates were performed.
- Run Playwright E2E/a11y and review regenerated visual baselines in the pinned test image.
- Obtain the user's design review before creating a PR, as requested.

The separately supplied offline HTML review bundles these production components and styles
with sample palette data. It does not contact the backend or submit login/registration data.
Its layout has not been visually verified in this session. The source-code tests exercise the
real save/login flow with API mocks; the HTML is for design review only.
