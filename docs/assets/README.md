# Screenshot & demo assets

Drop the images referenced by the root `README.md` here. Suggested set:

| File | What to capture |
|---|---|
| `home.png` | Home page — palette grid with tag filters and search |
| `admin.png` | Admin panel — the dynamic HEX-row colour editor with tag chips |
| `export.png` | Export page — a selected palette with the PNG preview |
| `demo.gif` | Short screen recording: search → open a palette → export |

Keep them reasonably sized (long edge ~1400px, PNG compressed) so the README stays light.
