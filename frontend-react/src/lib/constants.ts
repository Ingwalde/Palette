// The reserved handle that owns the seed catalogue (backend: models.CURATOR_HANDLE). A palette
// owned by it is shown as "Palette" rather than a username, and its URL is /u/palette/:slug.
export const CURATOR_HANDLE = "palette";
