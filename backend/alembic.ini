# Alembic config. The database URL is injected at runtime from app settings (env.py),
# so it is intentionally not stored here. script_location is overridden to an absolute
# path by the app bootstrap; the relative value here is for `alembic` CLI use from backend/.
[alembic]
script_location = alembic
prepend_sys_path = .
